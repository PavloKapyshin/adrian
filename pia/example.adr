struct Some(a) {
    field: a
}
struct None {}
adt Maybe(a) {
    Some(a), None
}

struct LLNode(a) {
    data: a
    next: Maybe(LLNode(a))
}

let a = LLNode(py#Int(1), Maybe.None())

py#print(a.data)
