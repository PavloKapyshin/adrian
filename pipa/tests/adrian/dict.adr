-- prelude#Dict tests
let users = {
    "John" = 1,
    "Mike" = 2,
    "Drake" = 3
}

print("Iteration")
for (name, id) in users {
    print(" ".join([name, toString(id)]))
}

print()

print("Dict.keys and Dict.values")
let names = users.keys()
let ids = users.values()
print(names)
print(ids)
