extension Vector(a) {
    fun map(func: Function(a, b)): Vector(b) {
        let result: Vector(b) = []
        for elem in self {
            result.append(func(elem))
        }
        return result
    }
}

fun addOne(n: Number): Number {
    return n + 1
}

print([1, 2, 3].map(addOne))
