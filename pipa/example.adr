print(data = io#argv[1:length(io#argv)])
print(", ".join(iterable = [1, 2, 3]))


struct EmptyDummyType {
    fun lol(element: String): String {
        return element
    }
}

print(EmptyDummyType().lol(element = "KOKOK"))
