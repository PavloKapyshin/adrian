fun factorialRecurse(n: Number): Number {
    if n in [0, 1] {
        return 1
    }
    return factorialRecurse(n - 1) * n
}

fun factorialLooped(n: Number): Number {
    var result = 1
    while n > 1 {
        result *= n
        n -= 1
    }
    return result
}

print(factorialLooped(10))
