protocol Node {
    fun generate(): String
}

struct SomeNode is Node {
    name: String

    fun generate(): String {
        let node = 1
        return self.name
    }
}

fun generate(node: Node): String {
    return node.generate()
}

print(generate(SomeNode("noob")))


let list = [1, 2, 3]
print(list)
list.append(4)
print(list)
