print(io#argv[1:length(io#argv)])
print(", ".join(iterable = [1, 2, 3]).split(by = ", "))

fun addSomething(elem1 first: a is (Addable), elem2 second: a): a {
    return elem1 + elem2
}

print(addSomething(1, second = 2))
